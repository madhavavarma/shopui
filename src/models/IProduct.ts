export interface IProduct {
  id: number;
  productName: string;
  weight: string;
  price: number;
  productDescription: string;
  image: string;
  categories: number[];
  subCategories: number[];
}
